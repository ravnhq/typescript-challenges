export type Absolute<T extends number | string | bigint > = `${T}` extends `${infer sign}${infer num}` ?
                                                            sign extends "-"|"+" ? num: `${T}` : never 